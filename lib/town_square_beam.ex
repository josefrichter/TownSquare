defmodule TownSquareBeam do
  @moduledoc """
  A BEAM port of TownSquare's realtime core.

  See `TownSquareBeam.Application` for the supervision tree, `TownSquareBeam.Scene`
  for the per-scene GenServer (presence + broadcast + seat arbitration), and
  `TownSquareBeam.Socket` for the WebSocket handler.
  """
end
